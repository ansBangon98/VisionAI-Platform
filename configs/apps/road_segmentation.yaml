application:
  name: Road Segmentation
  pipeline: segmentation
  enabled: false

source:
  name: parking_area

primary_model:
  name: road_segmentation
  task: segmentation
  model: models/segmentation/road/model.onnx
  backend: onnxruntime
  device: auto
  providers: []
  input_size: [640, 640]
  input_shape: [1, 3, 640, 640]
