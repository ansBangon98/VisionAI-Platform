application:
  name: Pose Estimation
  pipeline: pose
  enabled: false

source:
  name: office_entrance

primary_model:
  name: pose_estimation
  task: pose
  model: models/pose/pose_estimation/model.onnx
  backend: onnxruntime
  device: auto
  providers: []
  confidence_threshold: 0.4
  input_size: [640, 640]
  input_shape: [1, 3, 640, 640]
