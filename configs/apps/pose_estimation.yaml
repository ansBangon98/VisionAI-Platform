application:
  name: Pose Estimation
  enabled: false

source:
  name: office_entrance

pose_model:
  model: core/models/pose/pose_estimation.onnx
  backend: onnxruntime
  device: auto
  providers: []
  confidence_threshold: 0.4
  input_size: [640, 640]
  input_shape: [1, 3, 640, 640]
